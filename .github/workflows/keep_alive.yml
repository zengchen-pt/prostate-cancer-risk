name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'   # 每10分钟执行一次

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: curl -s -o /dev/null -w "%{http_code}" https://prostate-cancer-risk.onrender.com/predict
